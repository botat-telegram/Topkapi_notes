{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "name": "تشغيل البوت",
            "request": "launch",
            "command": "npm start",
            "cwd": "${workspaceFolder}"
        }
    ]
}
